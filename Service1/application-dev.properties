# Server Configuration
server.port=8080

# Database Configuration
spring.r2dbc.url=r2dbc:postgresql://localhost:5432/dev_db
spring.r2dbc.username=dev_user
spring.r2dbc.password=dev_password

# Logging
logging.level.root=DEBUG

# gRPC Configuration
grpc.server.port=9090

# External Config (Pull-based)
external.config.url=http://localhost:8081/config/dev

# Feature Toggle
feature.newFeatureEnabled=true
